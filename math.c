#include<stdio.h>
#include<math.h>

int main(){

    double d = sqrt(49);
    double f = pow(4,3);
    int g = round(49);
    int h = ceil(49);
    int i = floor(49);
    double k = fabs(-49);
    double l = log(3);
    double m = sin(45);
    double n = cos(45);
    double o = tan(45);

    printf("%lf\n", l);

    return 0;
}